@model MaerskLine_TP034179.ViewModels.ScheduleShipCustomerViewModel

@{
    ViewBag.Title = "SelectShip";
}

<h2>Select Ship</h2>

<table class="table">
    <tr>
        <th>
            Schedule ID
        </th>
        <th>
            Schedule Origin
        </th>
        <th>
            Schedule Destination
        </th>
        <th>
            Schedule Departure Time
        </th>
        <th>
            Schedule Arrival Time
        </th>
    </tr>
    <tr>
        <td>
            @Model.Schedule.ScheduleID
        </td>
        <td>
            @Model.Schedule.Origin
        </td>
        <td>
            @Model.Schedule.Destination
        </td>
        <td>
            @Model.Schedule.DepartureDateTime
        </td>
        <td>
            @Model.Schedule.ArrivalDateTime
        </td>
    </tr>
</table>


<table class="table">
    <tr>
        <th>
            Ship ID
        </th>
        <th>
            Ship Name
        </th>
        <th>
            Ship ContainerSpace
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.Ships)
    {
        <tr>
            <td>
                @item.ShipID
            </td>
            <td>
                @item.ShipName
            </td>
            <td>
                @item.ShipContainerSpace
            </td>
            <td>

                @Html.ActionLink("Select", "SelectCustomer", "Bookings", new { Shipid = item.ShipID, Scheduleid = @Model.Schedule.ScheduleID }, new { @class = "btn btn-success btn-xs" })
            </td>
        </tr>
    }
</table>